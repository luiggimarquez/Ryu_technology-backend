<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../../styles.css">
    <title>Path - Error</title>
</head>
<body>
    <header class="errorHeader"><h1>Ryu Technology</h1></header>
    <div class="errorBody">
        <h1 class="titleErrorDefault">500 Internal Server Error</h1>

        <div class="errorsContainer">
            <h2>Ha ocurrido un error</h2>
            <div class="errorContainer">
                <div class="errorTitle">
                    <h2 class="subTitleError">Error:</h2>
                    <p> <%= err %></p>
                </div>

                <div class="errorTitle" >
                    <h2 class="subTitleError">Detalles:</h2> 
                    <div class="itemsError"> 
                        <% error.forEach(items =>{ %>
                        <% for(const [key, value] of Object.entries(items)){%>
                            <li><%= key %> : <%= value %></li> 
                            <% } %>
                        <% }) %>
                    </div>
                </div>
            
            </div>
        </div>
    </div>

</body>

</html>